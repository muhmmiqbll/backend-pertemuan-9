const user = {
    name : "Muhammad iqbal",
    age : 20,
    address : "tangsel",
    collegeStudent : true,
};

const {name, age, address, collegeStudent} = user;
console.log(name, age, address, collegeStudent);